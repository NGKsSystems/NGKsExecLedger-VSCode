# NGKs ExecLedger Desktop UI - Quick Launch

## 🚀 Simple Launch Commands

### Option 1: Python Script (Cross-platform)
```bash
python launch_ui.py
```

### Option 2: Windows Batch File
```cmd
launch_ui.bat
```

### Option 3: Original Module Command
```bash
python -m desktop-companion.ui.app
```

## Requirements
- Python 3.8+
- PySide6: `pip install PySide6`

## What's New
- ✅ **Simplified Launch**: Just use `python launch_ui.py` instead of the complex module path
- ✅ **Windows Batch**: Double-click `launch_ui.bat` for instant launch
- ✅ **Error Handling**: Clear error messages if dependencies are missing
- ✅ **Auto Path Setup**: No need to manually configure Python paths

The desktop UI now uses the new **"execledger"** canonical path system from the recent global migration.